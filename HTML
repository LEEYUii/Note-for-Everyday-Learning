HTML:

如果要键入多个空格，添加“&nbsp”;

target attribute:

it used in the <a> (anchor element): <a href=”www.qwe.com” target=”_blank/_self”></a>;
_blank: means the link will be open on a new window or tab;（在新的页面打开）
_self: means the link will be open on the same frame as it been clinked;（在原页面打开）
_top: means the link will be open on a new browser;（在新的浏览器中打开）
_parent: means the link will be open on the parent frame;（在母级页面打开）
